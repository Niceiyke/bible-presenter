name: CI Check
on:
  pull_request:
  push:
    branches: [ main ]

jobs:
  frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: setup node
        uses: actions/setup-node@v4
        with:
          # undici 8.10 (via jsdom/vitest) requires node:worker_threads
          # markAsUncloneable, which only exists on Node >= 24.
          node-version: 24
          cache: npm

      - name: install frontend dependencies
        run: npm ci

      - name: typecheck + build
        run: npm run build

      # WP11 (P2-5): the packaged installer maps `dist` into the bundle, and the
      # remote Control browser bundle is a separate entry. Verify it is actually
      # produced so a release can never ship without the remote surface.
      - name: verify remote bundle is built
        run: test -f dist/remote.html

      - name: run frontend tests
        run: npm run test

  license-worker:
    # WP11 (P2-5): the license worker validates keys/device-slots server-side;
    # its tests gate the release just like the frontend/backend suites.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: setup node
        uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: install worker dependencies
        working-directory: workers/license
        run: npm ci

      - name: run license worker tests
        working-directory: workers/license
        run: npm test

  backend:
    # The app ships only for Windows (NSIS/WebView2); Tauri's glib/gtk -sys
    # crates also fail to pkg-config on bare ubuntu runners. Build/check on the
    # actual target platform instead of installing the whole Linux GTK stack.
    # ffmpeg-next is now default (pipe removed) — this job always builds the
    # in-process libav path (needs LLVM + vcpkg ffmpeg).
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: install rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy

      # tauri-build validates bundled resources at compile time, but dist/ and
      # the wordlyte-engine binary are gitignored (produced by the build).
      # Create placeholder so cargo check/clippy/test can run as a fast
      # compile gate; the release workflow produces the real artifact.
      - name: create bundled-resource placeholders for tauri-build
        shell: bash
        run: |
          mkdir -p src-tauri/binaries dist
          touch src-tauri/binaries/wordlyte-engine.exe dist/.keep

      - name: Install LLVM (bindgen for ffmpeg-sys-next)
        shell: pwsh
        run: |
          if (-not (Get-Command clang -ErrorAction SilentlyContinue)) {
            choco install llvm -y --no-progress
            echo "C:\Program Files\LLVM\bin" | Out-File -FilePath $env:GITHUB_PATH -Append
          }
          clang --version

      - name: Install pkg-config
        shell: pwsh
        run: |
          choco install pkgconfiglite -y --no-progress
          $pkg = "C:\ProgramData\chocolatey\bin\pkg-config.exe"
          if (Test-Path $pkg) { echo "pkg-config at $pkg" } else { Get-Command pkg-config | Format-List * }
          & $pkg --version
          echo "PKG_CONFIG=$pkg" | Out-File -FilePath $env:GITHUB_ENV -Append
          # Prepend chocolatey bin so it wins over Strawberry's pkg-config.bat
          echo "C:\ProgramData\chocolatey\bin" | Out-File -FilePath $env:GITHUB_PATH -Append

      - name: Install ffmpeg via vcpkg (system vcpkg at C:\vcpkg)
        shell: pwsh
        run: |
          vcpkg --version
          if (-not (Test-Path "C:\vcpkg\installed\x64-windows\lib\pkgconfig\libavutil.pc")) {
            vcpkg install ffmpeg:x64-windows --clean-after-build
          } else {
            echo "ffmpeg already installed in C:\vcpkg"
          }
          # Use forward slashes for PKG_CONFIG_PATH to avoid drive-letter colon split on msys pkg-config
          $pkgPath = "C:/vcpkg/installed/x64-windows/lib/pkgconfig"
          $env:PKG_CONFIG_PATH = $pkgPath
          $env:VCPKGRS_DYNAMIC = "1"
          $env:VCPKG_ROOT = "C:\vcpkg"
          # Prefer vcpkg's own pkgconf (handles Windows paths) over Strawberry
          $vcpkgPkgConf = Get-ChildItem C:\vcpkg\installed\x64-windows\tools\pkgconf -Recurse -Filter pkgconf.exe -ErrorAction SilentlyContinue | Select-Object -First 1 -ExpandProperty FullName
          if (-not $vcpkgPkgConf) { $vcpkgPkgConf = "C:\vcpkg\installed\x64-windows\tools\pkgconf\pkgconf.exe" }
          $env:PKG_CONFIG = $vcpkgPkgConf
          echo "PKG_CONFIG_PATH=$pkgPath" | Out-File -FilePath $env:GITHUB_ENV -Append
          echo "VCPKGRS_DYNAMIC=1" | Out-File -FilePath $env:GITHUB_ENV -Append
          echo "VCPKG_ROOT=C:\vcpkg" | Out-File -FilePath $env:GITHUB_ENV -Append
          echo "PKG_CONFIG=$vcpkgPkgConf" | Out-File -FilePath $env:GITHUB_ENV -Append
          echo "Using PKG_CONFIG=$vcpkgPkgConf"
          Get-Command pkg-config -ErrorAction SilentlyContinue | Select-Object Source
          & $env:PKG_CONFIG --modversion libavutil
          if ($LASTEXITCODE -ne 0) { throw "pkg-config failed in install step" }
          & $env:PKG_CONFIG --modversion libavformat

      - name: Verify pkg-config can find libavutil
        shell: pwsh
        run: |
          echo "PKG_CONFIG_PATH=$env:PKG_CONFIG_PATH"
          echo "PKG_CONFIG=$env:PKG_CONFIG"
          echo "VCPKG_ROOT=$env:VCPKG_ROOT"
          Test-Path $env:VCPKG_ROOT\.vcpkg-root
          Get-ChildItem C:/vcpkg/installed/x64-windows/lib/pkgconfig/libav*.pc | Select-Object Name
          & $env:PKG_CONFIG --modversion libavutil
          & $env:PKG_CONFIG --cflags --libs libavutil
          & $env:PKG_CONFIG --modversion libavformat
          & $env:PKG_CONFIG --cflags --libs libavformat

      - name: cargo check
        working-directory: src-tauri
        env:
          PKG_CONFIG_PATH: "C:/vcpkg/installed/x64-windows/lib/pkgconfig"
          PKG_CONFIG: "C:\vcpkg\installed\x64-windows\tools\pkgconf\pkgconf.exe"
          VCPKG_ROOT: "C:\\vcpkg"
          VCPKGRS_DYNAMIC: "1"
        run: cargo check

      - name: cargo clippy
        working-directory: src-tauri
        env:
          PKG_CONFIG_PATH: "C:/vcpkg/installed/x64-windows/lib/pkgconfig"
          PKG_CONFIG: "C:\vcpkg\installed\x64-windows\tools\pkgconf\pkgconf.exe"
          VCPKG_ROOT: "C:\\vcpkg"
          VCPKGRS_DYNAMIC: "1"
        run: cargo clippy --all-targets -- -D warnings

      - name: cargo test
        working-directory: src-tauri
        env:
          PKG_CONFIG_PATH: "C:/vcpkg/installed/x64-windows/lib/pkgconfig"
          PKG_CONFIG: "C:\vcpkg\installed\x64-windows\tools\pkgconf\pkgconf.exe"
          VCPKG_ROOT: "C:\\vcpkg"
          VCPKGRS_DYNAMIC: "1"
        run: cargo test
